<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Editar Perro</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-xl mx-auto bg-white p-6 rounded shadow">
    <h2 class="text-2xl font-bold mb-4 text-center">Editar Información del Perro</h2>

    <form action="/admin/edit/<%= dog._id %>" method="POST" enctype="multipart/form-data" class="space-y-4">
      <input type="text" name="name" value="<%= dog.name %>" placeholder="Nombre del perro" required class="w-full border border-gray-300 p-2 rounded" />
      <input type="text" name="owner" value="<%= dog.owner %>" placeholder="Dueño" required class="w-full border border-gray-300 p-2 rounded" />
      <input type="email" name="email" value="<%= dog.email %>" placeholder="Correo del dueño" required class="w-full border border-gray-300 p-2 rounded" />
      <input type="text" name="phone" value="<%= dog.phone %>" placeholder="Teléfono" class="w-full border border-gray-300 p-2 rounded" />
      <input type="text" name="breed" value="<%= dog.breed %>" placeholder="Raza" class="w-full border border-gray-300 p-2 rounded" />
      <input type="text" name="food" value="<%= dog.food %>" placeholder="Comida favorita" class="w-full border border-gray-300 p-2 rounded" />
      <input type="text" name="illnesses" value="<%= dog.illnesses %>" placeholder="Enfermedades" class="w-full border border-gray-300 p-2 rounded" />

      <% if (dog.image) { %>
        <p class="text-sm text-gray-600">Imagen actual:</p>
        <img src="<%= dog.image %>" alt="Imagen actual" class="mb-4 rounded shadow h-40 w-full object-cover">
      <% } %>
      <input type="file" name="image" class="w-full" />

      <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition">
        Guardar Cambios
      </button>
    </form>
  </div>
</body>
</html>

